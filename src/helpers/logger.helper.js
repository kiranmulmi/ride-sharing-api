const log = (message) => {
    console.log(message);
}

const info = (message) => {
    console.log(message);
}

const error = (message) => {
    console.log(message);
}

module.exports = {
    log,
    info,
    error
}
